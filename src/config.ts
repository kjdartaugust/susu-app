// Base URL of the shared Dola backend this app authenticates against. The susu
// data (circles, goals) and the user account live there; see /api/susu/* and
// /api/auth/* in the virtual-dollar-card repo.
export const API_BASE = "https://virtual-dollar-card.vercel.app";
